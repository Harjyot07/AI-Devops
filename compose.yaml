services:
  backend:
    # ... existing
  
  frontend:
    # ... existing
  
  prometheus:
    # ... existing
  
  grafana:
    # ... existing
  
  # 🆕 FREE LOCAL AI
  ollama:
    image: ollama/ollama:latest
    volumes:
      - ollama-data:/root/.ollama
    ports:
      - "11434:11434"
    environment:
      - OLLAMA_ORIGINS=*
    # For GPU support (optional):
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]

volumes:
  ollama-data:
